name: "CodeQL Advanced dotnet"

on:
  workflow_call:
    inputs:
      gh_runner:
        required: false
        type:     string
      node_version:
        required: true
        type: string 
      build_location:
        required: false
        type: string
      build_location_name:
        required: false
        type: string

jobs:
  analyze:
    name: Analyze csharp
    runs-on:
      group: ${{ inputs.gh_runner }}

    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    steps:
    - name: Set up code ql
      run: echo 'Hello I'm not ready yet'
